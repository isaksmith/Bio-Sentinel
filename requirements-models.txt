# Phase 2: Real model dependencies (heavier)
# Keep separate so core framework stays lightweight.
ultralytics>=8.0
torch>=2.0
torchvision>=0.15
supervision>=0.17
wget

# PyTorchWildlife — install from the forked repo or PyPI
# pip install git+https://github.com/isaksmith/PyTorchWildlifeCameraTraps.git
